redis/default:
  endpoint: "localhost:6379"
  transport: "tcp"
  db: 0
  tls:
    insecure: true
redis/custom:
  endpoint: "redis:6379"
  transport: "unix"
  db: 1
  tls:
    insecure: false
  username: "username"
  password: "password"
